import React from 'react';

const DemoUseCallback = () => {
  return <div>DemoUseCallback</div>;
};

export default DemoUseCallback;

// Viết lại 1 useMemo => useCache(callback, deps)
// Viết lại 1 useCallback
// map, filter, reduce, find, findIndex
// customMap